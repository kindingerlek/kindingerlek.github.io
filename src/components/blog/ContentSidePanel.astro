---
interface Props {
	visible: boolean
}

const { visible = false } = Astro.props as Props
---

{
	visible && (
		<aside class='sticky top-20 order-2 -me-28 hidden basis-80 lg:flex lg:flex-col'>
			<slot />
		</aside>
	)
}
